extends layout

block content

    h1 Chat General

    ul#messages

    form#chatForm
        input(
            type="text"
            id="messageInput"
            placeholder="Escribí un mensaje..."
            required
        )

        button(type="submit")
            | Enviar

    script(src="/socket.io/socket.io.js")
    script(src="/js/chat.js")